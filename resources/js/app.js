/*

require('./bootstrap');
const conversationId = 1; // Replace with the desired conversation ID

window.Echo.channel(`conversation.${conversationId}`)
  .listen('NewMessage', (e) => {
    alert("heeey");
  });
*/


